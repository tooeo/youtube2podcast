name: Test and CI v2

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main ]

jobs:
  test:
    runs-on: ubuntu-22.04
    strategy:
      matrix:
        # Updated Python versions - removed 3.9, using 3.10, 3.11, 3.12
        python-version: ['3.10', '3.11', '3.12']

    steps:
    - uses: actions/checkout@v4
    
    - name: Clear GitHub Actions cache
      run: |
        echo "Clearing any cached Python versions..."
        rm -rf ~/.cache/pip || true
        rm -rf ~/.local/lib/python* || true
    
    - name: Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@v4
      with:
        python-version: ${{ matrix.python-version }}
        check-latest: true
        cache: 'pip'
    
    - name: Debug Python version
      run: |
        echo "Requested Python version: ${{ matrix.python-version }}"
        python --version
        which python
    
    - name: Clear Python cache
      run: |
        python -m pip cache purge || true
    
    - name: Install FFmpeg
      run: |
        sudo apt-get update
        sudo apt-get install -y ffmpeg
    
    - name: Install dependencies
      run: |
        python --version
        python -m pip install --upgrade pip
        pip install -r requirements.txt
        pip install pytest pytest-cov black flake8
    
    - name: Lint with flake8
      run: |
        flake8 . --count --select=E9,F63,F7,F82 --show-source --statistics || true
        flake8 . --count --exit-zero --max-complexity=10 --max-line-length=88 --statistics || true
    
    - name: Check code formatting with black
      run: |
        black --check --diff . || true
    
    - name: Test imports
      run: |
        python -c "from config import Source, SourceType, Subscription, Config, ConfigManager; print('✅ Config imports OK')"
        python -c "import multi_downloader; print('✅ Multi-downloader import OK')"
    
    - name: Test with pytest
      run: |
        python -m pytest -v

  docker:
    runs-on: ubuntu-22.04
    needs: test
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Build Docker image
      run: |
        docker build -t youtube2podcast .
    
    - name: Test Docker image basic functionality
      run: |
        echo "🧪 Тестирование базовой функциональности Docker образа..."
        
        # Проверяем версию Python
        echo "📋 Проверка версии Python:"
        docker run --rm youtube2podcast python --version
        
        # Проверяем доступность основных модулей
        echo "📋 Проверка импорта модулей:"
        docker run --rm youtube2podcast python -c "
        try:
            from config import Source, SourceType, Subscription, Config, ConfigManager
            print('✅ Config imports OK')
        except ImportError as e:
            print(f'❌ Config import failed: {e}')
            exit(1)
        
        try:
            import multi_downloader
            print('✅ Multi-downloader import OK')
        except ImportError as e:
            print(f'❌ Multi-downloader import failed: {e}')
            exit(1)
        "
        
        # Проверяем доступность yt-dlp
        echo "📋 Проверка yt-dlp:"
        docker run --rm youtube2podcast python -c "
        try:
            import yt_dlp
            print(f'✅ yt-dlp version: {yt_dlp.version.__version__}')
        except ImportError as e:
            print(f'❌ yt-dlp import failed: {e}')
            exit(1)
        "
        
        # Проверяем доступность FFmpeg
        echo "📋 Проверка FFmpeg:"
        docker run --rm youtube2podcast ffmpeg -version | head -1
        
        echo "✅ Все базовые тесты Docker образа прошли успешно!"
    
    - name: Test Docker image with SKIP_DOWNLOAD
      run: |
        echo "🧪 Тестирование с SKIP_DOWNLOAD=true..."
        
        # Тестируем с переменной SKIP_DOWNLOAD=true
        echo "📋 Тест с предотвращением загрузки:"
        docker run --rm \
          -e SKIP_DOWNLOAD=true \
          -v $(pwd)/config.yaml.dist:/app/config.yaml \
          youtube2podcast python -c "
        import multi_downloader
        from config import ConfigManager
        
        try:
            config_manager = ConfigManager()
            print('✅ ConfigManager инициализирован успешно')
            
            # Проверяем, что переменная окружения работает
            import os
            skip_download = os.environ.get('SKIP_DOWNLOAD', 'false').lower()
            print(f'📋 SKIP_DOWNLOAD={skip_download}')
            
            if skip_download in ('true', '1', 'yes'):
                print('✅ Переменная SKIP_DOWNLOAD корректно установлена')
            else:
                print('❌ Переменная SKIP_DOWNLOAD не установлена')
                exit(1)
                
        except Exception as e:
            print(f'❌ Тест failed: {e}')
            exit(1)
        "
        
        echo "✅ Тесты с SKIP_DOWNLOAD прошли успешно!"
